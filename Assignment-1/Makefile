gsh:
	mkdir /gsh/
	cp cat /gsh/
	cp date /gsh/
	cp ls /gsh/
	cp mkdir /gsh/
	cp rm /gsh/

	gcc shell.c -o shell
	./shell

	rm -r /gsh/
	clear

run:
	gcc shell.c -o shell
	./shell
	clear

gsh-clean:
	rm -r /gsh/
